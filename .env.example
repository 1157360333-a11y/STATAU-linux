# ============================================================================
# STATAU 环境配置文件
# ============================================================================
# 说明：
#   1. 复制此文件为 .env 文件
#   2. 根据运行环境修改 RUN_MODE 的值
#   3. 本地开发：RUN_MODE=local
#   4. 服务器部署：RUN_MODE=server
# ============================================================================

# ----------------------------------------------------------------------------
# 运行模式配置（必填）
# ----------------------------------------------------------------------------
# 可选值：local（本地版） 或 server（服务器版）
RUN_MODE=local

# ----------------------------------------------------------------------------
# Flask 环境配置（可选）
# ----------------------------------------------------------------------------
# 可选值：development（开发环境） 或 production（生产环境）
# 如果不设置，会根据 RUN_MODE 自动选择：
#   - local 模式默认使用 development
#   - server 模式默认使用 production
# FLASK_ENV=development

# ----------------------------------------------------------------------------
# 数据库配置（可选）
# ----------------------------------------------------------------------------
# 如果不设置，默认使用 SQLite 数据库
# DATABASE_URL=sqlite:///instance/statau.db

# ----------------------------------------------------------------------------
# 密钥配置（可选）
# ----------------------------------------------------------------------------
# 生产环境建议设置一个随机的密钥
# SECRET_KEY=your-secret-key-here

# ----------------------------------------------------------------------------
# 邮件服务器配置（可选）
# ----------------------------------------------------------------------------
# MAIL_SERVER=smtp.qq.com
# MAIL_PORT=587
# MAIL_USERNAME=your_email@qq.com
# MAIL_PASSWORD=your_email_auth_code
